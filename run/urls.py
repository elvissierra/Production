from django.urls import path
from . import views

urlpatterns = [
    path(login)
    path(register)
    path(logout)
    path(map_view)
    path(area_view)
    path(person_view)
    path(tasks_view)
    path(tools_view)
    path(price_overview)
    path(organize_view)
]